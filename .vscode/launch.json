{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Flet Task",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "preLaunchTask": "run flet"
        }
    ]
}